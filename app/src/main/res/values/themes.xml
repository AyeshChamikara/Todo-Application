<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TodayTask" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/red</item> <!-- Define your custom primary color -->
        <item name="android:statusBarColor">@color/custom_status_bar</item>
    </style>

    <style name="Theme.TodayTask" parent="Base.Theme.TodayTask" />

    <style name="customTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/red</item> <!-- Define your custom primary color -->
        <item name="android:statusBarColor">@color/custom_status_bar</item>
    </style>

</resources>
